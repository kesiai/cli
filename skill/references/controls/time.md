# time

时间选择器。

## 字段定义

```json
{
  "key": "startTime",
  "type": "string",
  "controlType": "time",
  "title": "开始时间"
}
```

## 记录值示例

```json
{ "startTime": "14:30:00" }
```

## 说明

- 值为时间字符串，格式通常为 `HH:mm:ss`
- 适用于只需时间不需日期的场景（如排班时间）
