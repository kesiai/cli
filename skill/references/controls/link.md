# link

链接输入。

## 字段定义

```json
{
  "key": "website",
  "type": "string",
  "controlType": "link",
  "title": "网站"
}
```

## 记录值示例

```json
{ "website": "https://example.com" }
```

## 说明

- 与 `text` 结构一致，区别是前端渲染为超链接输入框
- 值为 URL 字符串
