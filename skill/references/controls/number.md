# number

数字输入。

## 字段定义

```json
{
  "key": "age",
  "type": "number",
  "controlType": "number",
  "title": "年龄"
}
```

## 记录值示例

```json
{ "age": 42 }
```

## 说明

- 对应 JSON `number` 类型（整数或浮点数）
- 可配合 `config` 设置 min/max/step/precision 等约束
