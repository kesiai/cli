# rate

评分组件。

## 字段定义

```json
{
  "key": "score",
  "type": "number",
  "controlType": "rate",
  "title": "评分"
}
```

## 记录值示例

```json
{ "score": 4 }
```

## 说明

- `type` 为 `"number"`，值为整数（通常 1-5）
- 前端渲染为星型评分组件
