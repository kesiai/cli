# area

地区选择器（省/市/区）。

## 字段定义

```json
{
  "key": "region",
  "type": "string",
  "controlType": "area",
  "title": "地区"
}
```

## 记录值示例

```json
{ "region": "110000" }
```

## 说明

- 值为行政区划编码（6 位数字字符串）
- 如 `110000`（北京市）、`310000`（上海市）、`440000`（广东省）
- 前端渲染为省市区三级联动选择器
