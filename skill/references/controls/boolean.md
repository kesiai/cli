# boolean

布尔开关。

## 字段定义

```json
{
  "key": "enabled",
  "type": "boolean",
  "controlType": "boolean",
  "title": "是否启用"
}
```

## 记录值示例

```json
{ "enabled": true }
```

## 说明

- 值为 `true` 或 `false`
- 常用于开关状态、是否激活等场景
